import Loader from "@/components/Loader/Loader";

// Цей файл автоматично відображається Next.js під час завантаження даних для будь-якого маршруту
export default function Loading() {
  return <Loader />;
}
